# Environment variables — NEVER commit this
.env

# Python virtual environment
venv/
env/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# HuggingFace model cache (large files, re-downloads automatically)
.cache/
*.bin
*.safetensors

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Test data / user responses (privacy)
data/responses/
data/raw/

# App runtime artifacts
*.db
data/chroma/
data/eval/*
# ...except hand-written evaluation source data (not derived):
!data/eval/crisis_testset.jsonl
!data/eval/crisis_testset_en.jsonl
!data/eval/crisis_testset_heldout.jsonl
!data/eval/retrieval_queries.jsonl
!data/eval/retrieval_queries_implicit.jsonl
# Hand-made ratings are source data, not derived: the human validation sheet
# (commit it once rated) and the second-model rating behind RESULTS.md §4b.
!data/eval/faithfulness_rating_sheet.csv
!data/eval/faithfulness_rating_key.csv
!data/eval/faithfulness_second_rater.csv
!data/eval/faithfulness_second_rater_sheet.csv
!data/eval/faithfulness_second_rater_key.csv
.pytest_cache/
models/
*.log

# Tooling caches and coverage output (regenerated on every run)
.coverage
.coverage.*
htmlcov/
.ruff_cache/
.mypy_cache/

# Editor-local agent permissions (machine-specific paths, not project config)
.claude/settings.local.json

# Editor backups
*.bak
