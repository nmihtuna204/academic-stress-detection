# Core web stack
fastapi>=0.115
uvicorn[standard]>=0.30
streamlit>=1.39
httpx>=0.27

# Data / validation / config
pydantic>=2.7
pydantic-settings>=2.2
SQLAlchemy>=2.0

# NLP
transformers>=4.44
torch>=2.3

# LLM orchestration + RAG
langchain>=0.3
langchain-openai>=0.2
langchain-community>=0.3
openai>=1.40
chromadb>=0.5
# Supplies the multilingual embedder used by app/rag/store.py. Without it the
# store falls back to Chroma's English-leaning default ONNX MiniLM with only a
# log warning - same 384 dims, so no error surfaces and retrieval silently
# degrades against an index built with the multilingual model.
sentence-transformers>=3.0

# Evaluation / plotting
scikit-learn>=1.5
matplotlib>=3.9
pandas>=2.2
numpy>=1.26
plotly>=5.22
# Backs every DataFrame.to_markdown() call in app/eval (compare, ablation);
# its absence crashes the runners at the final write step.
tabulate>=0.9

# Testing
pytest>=8.2
pytest-asyncio>=0.23

# Report and slide generation (report/build_*.py)
python-docx
python-pptx
